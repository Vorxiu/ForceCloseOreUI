name: Build

on:
  push:
  pull_request:

jobs:
  build-windows:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v5

      - name: Setup xmake
        uses: xmake-io/github-action-setup-xmake@v1

      - name: Build for Windows (DLL)
        run: |
          xmake f -p windows -m release -v -y
          xmake

      - name: Upload DLL
        uses: actions/upload-artifact@v6
        with:
          name: windows
          path: build/windows/x64/release/*.dll

  build-android:
    runs-on: windows-latest
    strategy:
      matrix:
        arch: [arm64-v8a, x86_64]

    steps:
      - uses: actions/checkout@v5

      - name: Setup xmake
        uses: xmake-io/github-action-setup-xmake@v1

      - name: Setup Android SDK
        uses: android-actions/setup-android@v3

      - name: Build for Android (SO)
        run: |
          xmake f -p android -a ${{ matrix.arch }} -m release -v -y
          xmake
      
      - name: Upload SO
        uses: actions/upload-artifact@v6
        with:
          name: android-${{ matrix.arch }}
          path: build/android/${{ matrix.arch }}/release/*.so

















          
          xmake
          

      - name: Upload SO
        uses: actions/upload-artifact@v6
        with:
          name: android
          path: build/android/arm64-v8a/release/*.so
